name: Build and deploy Node.js app to Azure Web App - weather-wardrobe-linux

on:
  push:
    branches:
      - main
  workflow_dispatch:

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest

    steps:
      - name: 📥 Checkout code
        uses: actions/checkout@v4

      - name: 🛠 Setup Node.js
        uses: actions/setup-node@v3
        with:
          node-version: '20.x'

      - name: 📦 Install frontend dependencies
        run: npm install
        working-directory: ./frontend

      - name: ⚙️ Build React app and move to backend
        run: |
          rm -rf ../backend/frontend
          mkdir -p ../backend/frontend
          npm run build
          mv build/* ../backend/frontend/
        working-directory: ./frontend

      - name: 📦 Install backend dependencies
        run: npm install
        working-directory: ./backend

      - name: 🚀 Deploy to Azure Web App
        uses: azure/webapps-deploy@v3
        with:
          app-name: 'weather-wardrobe-linux'
          publish-profile: ${{ secrets.AZUREAPPSERVICE_PUBLISHPROFILE_72386D3BF24E46C5BDA0F5ECED4B18A9 }}
          package: ./backend
